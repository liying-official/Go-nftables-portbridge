{
 "认证请求过于频繁，请稍后重试": "Too many authentication requests; try again later",
 "管理员令牌无效": "Invalid administrator token",
 "跨站请求被拒绝": "Cross-site request rejected",
 "CSRF 校验失败，请刷新页面": "CSRF validation failed; reload the page",
 "tls_min_version 仅支持 1.2 或 1.3": "tls_min_version only supports 1.2 or 1.3",
 "请先配置 TLS 并重启服务，再通过 HTTPS 启用严格 IP 白名单": "Configure TLS and restart first, then enable the strict IP allowlist over HTTPS",
 "严格 IP 白名单必须包含当前客户端地址": "The strict IP allowlist must include the current client address",
 "Content-Type 必须是 application/json": "Content-Type must be application/json",
 "JSON 请求体不能超过 ": "JSON body must not exceed ",
 " 字节": " bytes",
 "JSON 格式错误: ": "Invalid JSON: ",
 "请求只能包含一个 JSON 对象": "The request must contain exactly one JSON object"
}
