# v2.4.9

- 新增逐规则、有界选择性 ACL 证明与原始 tuple 的 flowtable 隔离。
- 修复首次登录、退出、令牌失效后的 GUI 恢复错误，正确显示当前进程 HTTPS 证书状态。
- 中英包默认 GUI、安装提示与当前文档分别本地化；正式签名打包时按语言分别编译嵌入界面。
- 兼容真实单设备 nft JSON 字符串，仍严格核对设备集合。
- 将批量系统调用的 `-1 + error` 保留为零完成量及原始错误；暂时发送压力不销毁有效 UDP 会话，未发送包计入丢弃，不新增重试队列。

源码包未签名，不是发布者签名二进制。部署前请阅读[转发限制](docs/forwarding-limits.md)。
